# Root helper only. Real mod version lives in each workspace gradle.properties
# and must stay identical (see NightBeam MultiLoader version sync).
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
